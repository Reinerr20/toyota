SYSTEMD STATUS
===============

This repository does not contain a deployable systemd service unit. The former
guide used machine-specific user and directory paths and could not be verified
against a committed deployment file.

See docs/SETUP_AND_RUN.md for the verified run command, current deployment
limitations, and the requirements for a future reviewed service definition.
